# Oh My Posh

## Notes:

- The default profile is located at `$USERPROFILE/Documents/PowerShell/Microsoft.PowerShell_profile.ps1`.
  When replacing it with the version-controlled version, make sure the filename matches the original.
